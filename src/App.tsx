/**
 * @file App root component.
 *
 * NOTE: In this template, routing is handled by RouterProvider in main.tsx.
 * This App component is kept as a minimal shell. If you need a global
 * component that wraps ALL routes (e.g., a toast container), add it here
 * and reference it in the route tree.
 *
 * For most apps, this file can be removed — the router IS the app.
 */

export default function App() {
  return null;
}
